spring.application.name=stockportfolioanalyzer

spring.datasource.url=jdbc:mysql://localhost:3306/stock_portfolio
spring.datasource.username=root
spring.datasource.password=Root@12345

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

indianapi.base-url=https://stock.indianapi.in
indianapi.api-key=sk-live-VzoCfqDIA8yTWLvzPO1QVdZQaDPMwIopeER0HI3K
server.servlet.context-path=/api

# Override these outside local development. The fallback is deliberately for local use only.
jwt.secret=${JWT_SECRET:local-development-secret-change-before-production-32-bytes}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
server.error.include-message=always
server.error.include-binding-errors=always
